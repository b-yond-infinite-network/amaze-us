apiVersion: v1
kind: Service
metadata:
  name: {{ include "reminderapp.fullname" . }}
  labels:
    {{- include "reminderapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reminderapp.selectorLabels" . | nindent 4 }}
