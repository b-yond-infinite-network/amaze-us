image: gradle:jdk12
stages:
  - build

build spring:
    stage: build
    script:
          - cd microservice/challenge-1/backend
          - gradle clean build -x test
    artifacts:
        paths:
        - build/libs/backend-latest.jar
        expire_in: 60 minutes
    except:
        - tags
